# Supplied PPTX Evidence

The two PPTX files supplied by the user are retained in the Downloads folder and rendered here for review:

- `strategy/`: `12牛牛对标研究与启动策略.pptx`, 27 slides.
- `rules/`: `12牛牛游戏规则_第一视角教材_图解版.pptx`, 26 slides.

The decks are source material. Their content is labeled `SOURCE_CONFIRMED` when it is directly visible in the deck, but it does not override the user's implementation scope or the demo-only safety boundary.
