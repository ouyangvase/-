# Admin Requirements

Roles: `SUPER_ADMIN`, `OPERATIONS`, `FINANCE_VIEWER`, `SUPPORT`, `RISK_REVIEWER`, `CAMPAIGN_MANAGER`, `READ_ONLY`.

Every write must capture actor, reason, ticket/reference and before/after state. Historical events and journals are append-only.
