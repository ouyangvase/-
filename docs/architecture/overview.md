# Architecture Overview

`PostgreSQL` is the intended business source of truth and is reached through the server-side `pg` driver. `Redis` is for locks, cache and rate limits. Frontend state is presentation only. Telegram messages are notifications only. The game engine, ledger and API own business decisions. The Mini App uses the API/SSE stream as its authoritative, permission-filtered path and optionally uses Supabase Realtime Broadcast/Presence for public room fan-out and online presence; private participant messages never enter that public channel.

The local build contains Mini App, Admin, API, Bot and Worker surfaces. When `DATABASE_URL` is configured, the API persists Telegram identities, hashed HttpOnly sessions, device bindings, security PIN hashes, idempotency keys, round events, outbox events, audit records and balanced ledger journals to PostgreSQL; authenticated requests hydrate an isolated user runtime from PostgreSQL after cold starts, including the user's available-balance journal. Internal packet records and claims are row-locked and durable through migration `006-persistent-internal-packets.sql`; when the database is absent, the API uses an explicitly labelled demo fallback. Staging health is degraded until PostgreSQL is reachable. Round transitions append `round_events` through an expected-state conditional update, so stale concurrent transitions fail rather than overwrite the current state; successful transitions enqueue an `outbox_events` record and publish to the long-lived SSE stream at `/api/rounds/:id/realtime` (add `?snapshot=1` for a one-shot probe); the Worker owns the single-flight advancement boundary and advances safe deadline-driven states inside a Postgres transaction guarded by an advisory lock and `state_version` check.

Authentication is server-side Telegram `initData` verification followed by an expiring `HttpOnly` `p12_session` cookie. Reply-keyboard `/start` launches additionally receive a short-lived, one-time database-backed launch grant because that Telegram launch mode may not provide `WebAppInitData`. Device onboarding generates an ECDSA P-256 key pair and stores it through Telegram SecureStorage when available; the API binds the public key once and requires a controlled recovery path for replacement. If SecureStorage is unavailable, only the public key is retained in the local compatibility path and the UI warns that rebinding may be required.

Telegram’s `/start` handler and Menu Button are Bot API entry points. The Mini App is the only game client; the API is the authority for the state path `BANKER_BIDDING → BETTING → PACKET_SENT → CLAIMING → EVALUATING → SETTLING → ROUND_COMPLETE`. Webhook updates are inserted into `telegram_updates` and `outbox_events`; a successful direct reply marks the outbox item published, while failed delivery remains retryable by the Worker.
