{
  "buildCommand": "pnpm --filter @project12/miniapp build",
  "outputDirectory": "apps/miniapp/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
